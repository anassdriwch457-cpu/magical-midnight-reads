import { ChapterUrlUploadView } from "./chapter-url-upload";

export function MigratorView() {
  return (
    <div className="space-y-4 max-w-3xl">
      <div className="rounded-xl border border-dashed border-border bg-card/50 backdrop-blur p-6 text-sm text-muted-foreground">
        The migrator is not available yet.
      </div>
      <ChapterUrlUploadView />
    </div>
  );
}
