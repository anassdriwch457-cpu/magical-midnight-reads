// This file is automatically generated. Do not edit it directly.
import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "./types";

function getSupabaseUrl(): string {
  const url =
    (import.meta.env.VITE_SUPABASE_URL as string | undefined) ||
    (typeof process !== "undefined"
      ? (process.env?.SUPABASE_URL as string | undefined)
      : undefined);
  if (!url) {
    throw new Error("Missing VITE_SUPABASE_URL environment variable. Set it in your .env file.");
  }
  return url;
}

function getSupabaseKey(): string {
  const key =
    (import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY as string | undefined) ||
    (typeof process !== "undefined"
      ? (process.env?.SUPABASE_PUBLISHABLE_KEY as string | undefined)
      : undefined);
  if (!key) {
    throw new Error(
      "Missing VITE_SUPABASE_PUBLISHABLE_KEY environment variable. Set it in your .env file.",
    );
  }
  return key;
}

let _supabase: SupabaseClient<Database> | undefined;

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";
export const supabase = new Proxy({} as SupabaseClient<Database>, {
  get(_, prop, receiver) {
    if (!_supabase) {
      _supabase = createClient<Database>(getSupabaseUrl(), getSupabaseKey(), {
        auth: {
          storage: typeof window !== "undefined" ? localStorage : undefined,
          persistSession: true,
          autoRefreshToken: true,
        },
      });
    }
    return Reflect.get(_supabase, prop, receiver);
  },
});
